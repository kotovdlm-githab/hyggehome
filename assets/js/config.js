/* ============================================================
   hyggehome — глобальная конфигурация
   Здесь собраны все точки будущей интеграции с бэкендом.
   Пока бэкенда нет — поля пустые, сайт работает как витрина.
   ============================================================ */
window.HH_CONFIG = {
  // Базовый URL вашего API (бэкенда). Пока пусто — корзина хранится локально.
  // Пример в будущем: "https://api.hyggehome.ru"
  apiBase: "",

  // Имя Telegram-бренд-бота (без @). Используется для кнопки входа/уведомлений.
  // Пример: "hyggehome_bot"
  telegramBot: "",

  // Провайдер онлайн-оплаты в РФ. Варианты: "yookassa" | "cloudpayments" |
  // "robokassa" | "tinkoff" | "sbp". Реальное подключение требует бэкенда.
  paymentProvider: "yookassa",

  // Валюта и оформление цен.
  currency: "RUB",
  locale: "ru-RU",

  // Стоимость и порог бесплатной доставки (демо-значения).
  delivery: { price: 350, freeFrom: 3000 }
};
